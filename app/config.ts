export const API_URL = import.meta.env.VITE_API_URL || '';
export const RENT_AMOUNT = Number(import.meta.env.VITE_RENT_AMOUNT) || 0;
export const SAVINGS_AMOUNT = Number(import.meta.env.VITE_SAVINGS_AMOUNT) || 0;
